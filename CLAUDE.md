# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Contract Watchdog — an AI-powered contract monitoring and analysis tool. Currently in early setup; the real application has not been built yet.

## Repository

- **GitHub:** https://github.com/sscxzyx/contract-watchdog
- **Local path:** `C:\Users\jaime\contract watchdog`

## Git workflow

After every meaningful change:
1. Stage relevant files (never use `git add -A` blindly — exclude secrets and binaries)
2. Commit with a clear, concise message describing *why* the change was made
3. Push to `origin master`

Use `C:\Program Files\GitHub CLI\gh.exe` if `gh` is not on PATH.

## Stack

To be defined as the project is built. Update this file when the stack is decided.
